name: "Python HTTP Proxy"
version: "1.0.0"
slug: "python_http_proxy"
description: "Lightweight HTTP Forward Proxy for Python scripts"
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
init: false
options:
  auth_user: "dev_user"        # Thay bằng username của bạn
  auth_pass: "P@ssword123_S3cur3" # Thay bằng pass cực khó
schema:
  auth_user: str
  auth_pass: str
ports:
  8888/tcp: 8888
